using UnityEngine;

namespace Ivayami.Player
{
    public class LockFogSphere : MonoBehaviour
    {
        void Update()
        {
            transform.rotation = Quaternion.identity;
        }
    }
}